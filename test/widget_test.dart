// // ignore_for_file: unused_import

// import 'package:flutter/material.dart';
// import 'package:flutter_test/flutter_test.dart';
// import 'package:demo_project/main.dart'; // Ensure this import is correct
// import 'package:demo_project/home.dart'; // Ensure this import is correct

// void main() {
//   testWidgets('Home widget is displayed', (WidgetTester tester) async {
//     // Build the app and trigger a frame.
//     await tester.pumpWidget(const MyApp());

//     // Verify that the Home widget is displayed.
//     expect(find.byType(Home), findsOneWidget);

//     // Verify that the title of the app is displayed.
//     expect(find.text('Live Eye Stress Detection App'), findsOneWidget);

//     // Check for the buttons
//     expect(find.text('Select Image'), findsOneWidget);
//     expect(find.text('Take Photo'), findsOneWidget);

//     // Check for the default text indicating no predictions
//     expect(find.text('No prediction yet'), findsOneWidget);
//   });
// }
